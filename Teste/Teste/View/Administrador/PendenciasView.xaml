<UserControl x:Class="Teste.View.PendenciasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F8FAFC">

    <Grid Margin="30">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Cabeçalho -->
        <StackPanel Grid.Row="0" Margin="0,0,0,30">
            <TextBlock Text="Pendências"
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="#1F2937"/>

            <TextBlock Text="Pedidos aguardando confirmação de pagamento."
                       FontSize="15"
                       Foreground="#6B7280"
                       Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Cards -->
        <Grid Grid.Row="1" Margin="0,0,0,30">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="White"
                    CornerRadius="10"
                    Padding="20"
                    BorderBrush="#E5E7EB"
                    BorderThickness="1">

                <StackPanel>
                    <TextBlock Text="Pagamentos Pendentes"
                               Foreground="#6B7280"
                               FontSize="14"
                               FontWeight="SemiBold"/>

                    <TextBlock Text="{Binding TotalPendencias}"
                               FontSize="28"
                               FontWeight="Bold"
                               Foreground="#EF4444"
                               Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2"
                    Background="White"
                    CornerRadius="10"
                    Padding="20"
                    BorderBrush="#E5E7EB"
                    BorderThickness="1">

                <StackPanel>
                    <TextBlock Text="Valor Pendente"
                               Foreground="#6B7280"
                               FontSize="14"
                               FontWeight="SemiBold"/>

                    <TextBlock Text="{Binding ValorTotalPendente, StringFormat=C}"
                               FontSize="28"
                               FontWeight="Bold"
                               Foreground="#D97706"
                               Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

        </Grid>

        <!-- Lista de Pendências -->
        <Border Grid.Row="2"
                Background="White"
                CornerRadius="10"
                Padding="20"
                BorderBrush="#E5E7EB"
                BorderThickness="1">

            <DataGrid ItemsSource="{Binding PedidosPendentes}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True">

                <DataGrid.Columns>

                    <DataGridTextColumn Header="Pedido"
                                        Binding="{Binding IdPedido}"
                                        Width="80"/>

                    <DataGridTextColumn Header="Cliente"
                                        Binding="{Binding Recebedor}"
                                        Width="*"/>

                    <DataGridTextColumn Header="Data"
                                        Binding="{Binding DataDoPedido}"
                                        Width="150"/>

                    <DataGridTextColumn Header="Valor"
                                        Binding="{Binding Total, StringFormat=C}"
                                        Width="120"/>

                    <DataGridTemplateColumn Header="Ação" Width="180">

                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>

								<Button Content="Confirmar Pagamento"
        Command="{Binding RelativeSource={RelativeSource AncestorType=DataGrid},
                          Path=DataContext.ConfirmarPagamentoCommand}"
        CommandParameter="{Binding}" />

                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>

                    </DataGridTemplateColumn>

                </DataGrid.Columns>

            </DataGrid>

        </Border>

    </Grid>

</UserControl>