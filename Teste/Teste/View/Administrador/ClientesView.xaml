<UserControl x:Class="Teste.View.ClientesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F5F7FA">
    
    

    <Grid Margin="25">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        

        <!-- CABEÇALHO -->

        <Border Background="White"
                CornerRadius="12"
                Padding="20"
                Margin="0,0,0,20">
            
            

            <StackPanel>

                <TextBlock Text="👥 Clientes"
                           FontSize="30"
                           FontWeight="Bold"
                           Foreground="#1F2937"/>

                <TextBlock Text="Visualize todos os clientes cadastrados e seus históricos de pedidos."
                           FontSize="14"
                           Foreground="#6B7280"
                           Margin="0,5,0,0"/>

            </StackPanel>

        </Border>

        <!-- CONTEÚDO -->

        <Grid Grid.Row="1">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- LISTA DE CLIENTES -->

            <Border Grid.Column="0"
                    Background="White"
                    CornerRadius="12"
                    Padding="10">

                <DockPanel>

                    <TextBlock Text="Clientes Cadastrados"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Margin="10"
                               DockPanel.Dock="Top"/>

                    <ListBox ItemsSource="{Binding Clientes}"
         SelectedItem="{Binding ClienteSelecionado}"
         BorderThickness="0"
         Background="Transparent">

                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">

                                <Setter Property="Height" Value="80"/>
                                <Setter Property="Margin" Value="0,0,0,8"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>

                                <Setter Property="Template">
                                    <Setter.Value>

                                        <ControlTemplate TargetType="ListBoxItem">

                                            <Border x:Name="Bd"
                                Background="White"
                                BorderBrush="#E5E7EB"
                                BorderThickness="1"
                                CornerRadius="10"
                                Padding="10">

                                                <ContentPresenter/>

                                            </Border>

                                            <ControlTemplate.Triggers>

                                                <Trigger Property="IsSelected"
                                     Value="True">

                                                    <Setter TargetName="Bd"
                                        Property="Background"
                                        Value="#DBEAFE"/>

                                                    <Setter TargetName="Bd"
                                        Property="BorderBrush"
                                        Value="#3B82F6"/>

                                                </Trigger>

                                                <Trigger Property="IsMouseOver"
                                     Value="True">

                                                    <Setter TargetName="Bd"
                                        Property="BorderBrush"
                                        Value="#60A5FA"/>

                                                </Trigger>

                                            </ControlTemplate.Triggers>

                                        </ControlTemplate>

                                    </Setter.Value>
                                </Setter>

                            </Style>
                        </ListBox.ItemContainerStyle>

                        <ListBox.ItemTemplate>

                            <DataTemplate>

                                <Grid>

                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- FOTO -->

                                    <Border Width="50"
                        Height="50"
                        CornerRadius="25"
                        ClipToBounds="True"
                        VerticalAlignment="Center">

                                        <Image Source="{Binding FotoPerfil}"
                           Stretch="Fill"/>

                                    </Border>

                                    <!-- DADOS -->

                                    <StackPanel Grid.Column="1"
                            VerticalAlignment="Center">

                                        <TextBlock Text="{Binding Nome}"
                               FontWeight="Bold"
                               FontSize="14"
                               Foreground="#111827"
                               TextWrapping="NoWrap"
                               TextTrimming="CharacterEllipsis"/>

                                        <TextBlock Text="{Binding Email}"
                               FontSize="12"
                               Foreground="#6B7280"
                               TextWrapping="NoWrap"
                               TextTrimming="CharacterEllipsis"/>

                                    </StackPanel>

                                </Grid>

                            </DataTemplate>

                        </ListBox.ItemTemplate>

                    </ListBox>

                </DockPanel>

            </Border>

            <!-- HISTÓRICO -->

            <Border Grid.Column="2"
                    Background="White"
                    CornerRadius="12"
                    Padding="20">

                <Grid>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- TÍTULO -->

                    <TextBlock Text="Histórico de Pedidos"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="#1F2937"/>

                    <!-- DADOS DO CLIENTE -->

                    <Border Grid.Row="1"
                            Margin="0,15,0,15"
                            Background="#F9FAFB"
                            CornerRadius="8"
                            Padding="10">

                        <StackPanel>

                            <TextBlock Text="{Binding ClienteSelecionado.Nome}"
                                       FontWeight="Bold"
                                       FontSize="18"/>

                            <TextBlock Text="{Binding ClienteSelecionado.Email}"
                                       Foreground="Gray"/>

                            <TextBlock Text="{Binding ClienteSelecionado.Telefone}"
                                       Foreground="Gray"/>

                        </StackPanel>

                    </Border>

                    <!-- TABELA -->

                    <DataGrid Grid.Row="2"
                              ItemsSource="{Binding PedidosCliente}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              HeadersVisibility="Column">

                        <DataGrid.Columns>

                            <DataGridTextColumn Header="Pedido"
                                                Binding="{Binding IdPedido}"
                                                Width="100"/>

                            <DataGridTextColumn Header="Data"
                                                Binding="{Binding DataDoPedido}"
                                                Width="120"/>

                            <DataGridTextColumn Header="Status"
                                                Binding="{Binding Status}"
                                                Width="120"/>

                            <DataGridCheckBoxColumn Header="Pago"
                                                    Binding="{Binding Pago}"
                                                    Width="80"/>

                            <DataGridTextColumn Header="Total"
                                                Binding="{Binding Total,StringFormat=R$ {0:N2}}"
                                                Width="120"/>

                        </DataGrid.Columns>

                    </DataGrid>

                </Grid>

            </Border>

        </Grid>

    </Grid>

</UserControl>