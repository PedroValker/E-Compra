<UserControl x:Class="Teste.View.PedidosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Teste.ViewModel"
             Background="White">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2.5*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock x:Name="AbaHistoricoTexto" Text="Histórico de Pedidos" MouseLeftButtonDown="AbaHistorico_Click" Cursor="Hand" FontWeight="Bold" Foreground="Black" Margin="0,0,20,0"/>
            <TextBlock x:Name="AbaPendentesTexto" Text="Pedidos Pendentes" MouseLeftButtonDown="AbaPendentes_Click" Cursor="Hand" FontWeight="Normal" Foreground="Gray"/>
        </StackPanel>

        <Border Grid.Row="1" Grid.Column="0" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="10" Margin="0,0,20,0">
            <ListView x:Name="TabelaDePedidos" 
                      ItemsSource="{Binding ListaPedidosEntregues}" 
                      SelectedItem="{Binding PedidoSelecionado, Mode=TwoWay}"
                      BorderThickness="0" Background="Transparent" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Margin" Value="0,5"/>
                    </Style>
                </ListView.ItemContainerStyle>

                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Dia" Width="100">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding DataDoPedido}" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Produto" DisplayMemberBinding="{Binding NomePedido}" Width="150"/>

                        <GridViewColumn Header="Status do Pedido" Width="130">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Padding="10,4" CornerRadius="12" HorizontalAlignment="Left">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#E0E0E0"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="Pendente">
                                                        <Setter Property="Background" Value="#FFB74D"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Entregue">
                                                        <Setter Property="Background" Value="#00E676"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding Status}" FontWeight="SemiBold" FontSize="12" Foreground="Black" HorizontalAlignment="Center"/>
                                    </Border>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Qtd" Width="60">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="1" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="Valor" Width="100">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Total, StringFormat='R$ \{0:N2\}'}" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="" Width="100">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Ver Mais" 
                                            Click="VerDetalhes_Click"
                                            Background="#212121" Foreground="White" 
                                            Padding="15,5" BorderThickness="0" Cursor="Hand">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="15"/>
                                            </Style>
                                        </Button.Resources>
                                    </Button>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>

        <StackPanel Grid.Row="1" Grid.Column="1" DataContext="{Binding PedidoSelecionado}">

            <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="15" Background="White">
                <StackPanel>
                    <TextBlock Text="{Binding Total, StringFormat='Total: R$ \{0:N2\}'}" FontWeight="Bold" FontSize="18" Margin="0,0,0,10"/>
                    <TextBlock Text="Resumo do pedido:" FontStyle="Italic" FontSize="12" Margin="0,0,0,10"/>

                    <ScrollViewer MaxHeight="250" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding Itens}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="20"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Quantidade}" FontWeight="Bold" Grid.Column="0" FontSize="12"/>
                                        <TextBlock Text="{Binding Nome}" Grid.Column="1" TextTrimming="CharacterEllipsis" FontSize="12"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
            </Border>

            <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,15,0,0" Background="White">
                <StackPanel>
                    <TextBlock Text="Observações:" FontSize="12" Foreground="#757575" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding Observacoes}" TextWrapping="Wrap" FontSize="12" MinHeight="40"/>
                </StackPanel>
            </Border>

        </StackPanel>
    </Grid>
</UserControl>